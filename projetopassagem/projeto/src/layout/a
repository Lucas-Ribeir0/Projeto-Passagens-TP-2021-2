import React, {useState} from "react"
import "./voos.css"
import Botao from "../components/button"


const Voo = props => {
    const [valor, setValor] = useState(props.assentos);
    
    let estilo = {
        backgroundColor: props.color || '#DDD',
        borderRadius: props.bradius || '4px',
        boxShadow: props.bshadow || '-3px 4px 0 0 rgba(0, 0, 0, 0.2)',
    }
    
    
    let {idVoo, partida, destino, assentos} = props;
    let lugares
    
    const estadoInicial = {
        idVoo: props.idVoo,
        Partida: props.partida,
        Destino: props.destino,
        Assentos: props.assentos
    }

    let teste = "ABC"

    let state = {
        ...estadoInicial
    }
    function Reservar() {
        console.log(teste)
        teste = "BAC"
        console.log(teste)
        console.log(assentos)
        return teste
    }
    return(
        <div className="numVoo" style = {estilo}>
            <div className="idVoo"><h2>{props.idVoo}</h2></div>
            <div className="info">
                <p>Partida: {props.partida} </p>
                <p>Destino: {props.destino}</p>
                <p>Assentos Disponíveis: <span>{teste}</span></p>
            </div>
            <Botao label="Reservar" click={(Reservar)}/>
        </div>
    )
}

export default Voo